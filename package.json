{
  "name": "hsablonniere.com",
  "version": "1.0.0",
  "engines": {
    "node": "14"
  },
  "scripts": {
    "build": "eleventy && npm run compress-files",
    "serve": "eleventy --serve",
    "compress-files": "hititipi-compress './_site/**/*'",
    "start": "http-server -b -g -c5 -d=false _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.11.0",
    "hititipi": "^0.4.0",
    "http-server": "^0.12.3"
  }
}
